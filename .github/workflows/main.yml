name: Work Stats Readme

on:
  workflow_dispatch:
  schedule:
    # Runs every 2 hours
    - cron: "0 */2 * * *"

jobs:
  update-readme:
    name: MixFIx7's README
    runs-on: ubuntu-latest
    steps:
      - uses: MixFix7/README.md@main
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
